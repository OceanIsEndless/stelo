# stelo
Scratch-Translate-Extension-Languages Optimizer

## todo
For sake of completeness...
- allow reversing the process (paste optimized JSON, return original JSON)
- perhaps combine `codes` arrays if they are identical (idk if this happens or not)
- if going for a super squished JSON, convert all arrays to strings, omitting string quotation marks and such (less separators = less storage space)
- use more compact format or invent  custom format for super low storage cost
- ignore all of the above and just add the Translate extension to SE!!!! (this is why I made this)
